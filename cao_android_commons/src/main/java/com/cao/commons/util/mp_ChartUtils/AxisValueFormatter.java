package com.cao.commons.util.mp_ChartUtils;/** * Created by philipp on 02/06/16. * 显示 柱状图 折线图 X轴的数据 */public class AxisValueFormatter {//        implements IAxisValueFormatter {////    private List<String> nameList;////    public AxisValueFormatter(List<String> nameList) {//        this.nameList = nameList;//    }////    @Override//    public String getFormattedValue(float value, AxisBase axis) {//        int valueS = (int) value;////            LogUtils.logd(nameList.size() + " : " + (int) value);//        if (nameList != null && nameList.size() > 0 && valueS >= 0 && valueS < nameList.size()) {//            return nameList.get(valueS);//        } else//            return "";////    }}